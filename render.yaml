services:
  - type: web
    name: trumps-card-game
    runtime: static
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: ./trumps
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
